var React = require('react');

// A Stateless functional Component only works on components that don't maintain state

var About = (props) => {
  return (
    <h3>The New About Component</h3>
  )
};

module.exports = About;